{
  "name": "@trajct/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "db:migrate": "drizzle-kit migrate",
    "db:migrate:dry": "drizzle-kit migrate --dry-run",
    "db:studio": "drizzle-kit studio",
    "db:generate": "drizzle-kit generate",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.0",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@trajct/config": "workspace:*",
    "@types/node": "^22.19.19",
    "@vitest/coverage-v8": "^2.0.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  },
  "peerDependencies": {
    "postgres": "^3.4.0"
  }
}
