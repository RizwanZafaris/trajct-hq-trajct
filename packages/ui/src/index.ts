/**
 * @trajct/ui — Shared component library (Tailwind + shadcn/ui pattern).
 * Components are "owned code, not a dependency" (Technical-Methodology §2).
 * See README.md for adding new components.
 */
export * from "./tokens.js";
export { cn } from "./utils.js";
